// Define traducciones de estructuras y palabras clave de control de flujo en español.

#define inicio void setup(){      // Inicio de la función de configuración (equivalente a void setup())
#define bucle void loop(){        // Inicio de la función principal de ejecución (equivalente a void loop())
#define fin }                     // Fin de cualquier bloque de código (equivalente a })
#define cerrar }                  // Fin de cualquier bloque de código (equivalente a })

#define VERDADERO true            // Constante que representa el valor verdadero (equivalente a true)
#define FALSO false               // Constante que representa el valor falso (equivalente a false)
#define NULO NULL                 // Puntero nulo (equivalente a NULL)
#define CERO 0                    // Valor numérico cero (equivalente a 0)

#define si if (                   // Palabra clave para estructuras de control de flujo "if" (equivalente a if)
#define entonces ) {              // Inicio del bloque de código que sigue a la condición "if" (equivalente a {)
#define si_no } else {            // Bloque de código que sigue a la condición "else" (equivalente a } else {)
#define si_no_si } else if (      // Bloque de código que sigue a la condición "else if" (equivalente a } else if)
#define repetir(var, condition, interval) for (var; condition; interval){  // Estructura de repetición "for" (equivalente a for)
#define para_cada(element, array) for (auto &element : array){  // Bucle "for each" para recorrer elementos de un array.
#define mientras while (            // Palabra clave para bucles "while" (equivalente a while)
#define hacer do                   // Palabra clave para bucles "do-while" (equivalente a do)
#define elegir(i) switch(i){       // Palabra clave para estructuras de selección "switch" (equivalente a switch)
#define caso case                 // Definición de casos en una estructura "switch" (equivalente a case)
#define predeterminado default     // Definición de caso predeterminado en una estructura "switch" (equivalente a default)
#define continuar continue         // Palabra clave para saltar a la siguiente iteración en un bucle (equivalente a continue)
#define romper break;              // Palabra clave para salir de un bucle o estructura de control de flujo (equivalente a break)
#define devolver return            // Palabra clave para devolver un valor desde una función (equivalente a return)
#define parametros (              // Palabra clave para simplificar la declaración de parámetros de una función (equivalente a '(')

#define es_igual ==                   // Operador de igualdad (equivalente a ==)
#define es_diferente !=               // Operador de desigualdad (equivalente a !=)
#define y &&                      // Operador lógico AND (equivalente a &&)
#define o ||                      // Operador lógico OR (equivalente a ||)
#define no !                       // Operador lógico NOT (equivalente a !)
